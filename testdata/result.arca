import go.fmt
import go.strconv

fn parseAndDouble(s: String) -> Result[Int, error] {
  let n = strconv.Atoi(s)?
  Ok(n + n)
}

fn main() {
  fmt.Println(parseAndDouble("21"))
  fmt.Println(parseAndDouble("abc"))
}

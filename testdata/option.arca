import go "fmt"

fun greet(name: Option[String]) -> String {
  match name {
    Some(n) -> "Hello ${n}!"
    None -> "Hello stranger!"
  }
}

fun main() {
  fmt.Println(greet(Some("Alice")))
  fmt.Println(greet(None))
}

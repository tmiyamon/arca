import go.fmt

fn greet(name: Option[String]) -> String {
  match name {
    Some(n) -> "Hello ${n}!"
    None -> "Hello stranger!"
  }
}

fn main() {
  fmt.Println(greet(Some("Alice")))
  fmt.Println(greet(None))
}
